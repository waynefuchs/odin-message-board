extends layout

block content
  h1= title
  p Welcome to #{title}

  each message in messages
    div.message
      div.user #{message.user}
      div.date #{message.added.toLocaleDateString()}
      div.text #{message.text}
